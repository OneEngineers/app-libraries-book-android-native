query PodcastDetail($documentId: ID!) {
  podcast(documentId: $documentId) {
    documentId
    name
    description
    image {
      url
    }
    category {
      name
    }
    speaker {
      name
    }
  }
}
